import { DisplayModelDirective } from './display-model.directive';

describe('DisplayModelDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayModelDirective();
    expect(directive).toBeTruthy();
  });
});
